//setting margin top and bottom depending on margin left
// const wrapper = document.querySelector('.wrapper');
// function wrapperSetMargin() {
//   wrapper.style.marginTop = `${Math.min(wrapper.offsetLeft, 192)}px`;
//   wrapper.style.marginBottom = `${Math.min(wrapper.offsetLeft, 192)}px`;
// }
// if(wrapper && window.innerWidth >= 1536) {
//   wrapperSetMargin();
//   window.addEventListener('resize', wrapperSetMargin)
// }

